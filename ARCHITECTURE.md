# 📊 NOBApp 自動發佈系統 - 系統架構圖

## 🎯 整體流程圖

```
┌─────────────────────────────────────────────────────────────────┐
│    ▶️ 開始發佈流程                 │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│  🛠️ Visual Studio 或命令行執行 Publish    │
│  $ dotnet publish ... -c Release -p:PublishProfile=FolderProfile│
└─────────────────────────────────────────────────────────────────┘
              │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│  📦 MSBuild Publish 目標                │
│  ├─ 編譯應用程式         │
│  ├─ 發佈文件到 C:\BOT\PS  │
│  └─ 觸發 AfterTargets="Publish"        │
└─────────────────────────────────────────────────────────────────┘
              │
▼
┌─────────────────────────────────────────────────────────────────┐
│  🎬 CustomActionsAfterPublish 目標（在 .csproj 中定義）        │
└─────────────────────────────────────────────────────────────────┘
    │
               ┌───────────────┼───────────────┐
      │               │          │
   ▼▼            ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │        │  │    │  │          │
    │ 步驟 1️⃣     │  │ 步驟 2️⃣     │  │ 步驟 3️⃣ │
    │ 更新版本號   │  │ 打包成 ZIP   │  │ 上傳 GitHub  │
    │     │  │     │  │            │
    └──────────────┘  └──────────────┘  └──────────────┘
         │        │          │
 ▼         ▼      ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │ Update       │  │ 7-Zip        │  │ Git Commit   │
    │ Version.ps1  │  │ Compress     │  │ & Push       │
    │   │  │  │  │     │
    │ 0.84.3 →     │  │ C:\BOT\PS\ → │  │ GitHub │
    │ 0.84.4       │  │ v0.84.4.zip  │  │ Release      │
    └──────────────┘  └──────────────┘  └──────────────┘
 │             │     │
  ▼              ▼    ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │ Version      │  │ ZIP File   │  │ Release Tag  │
    │ Info.cs      │  │ C:\BOT\      │  │ v0.84.4      │
    │ 已更新 ✅    │  │ 已建立 ✅    │  │ 已上傳 ✅    │
    └──────────────┘  └──────────────┘  └──────────────┘
       │         │    │
└───────────────┼───────────────┘
    │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  ✅ 發佈流程完成              │
└─────────────────────────────────────────────────────────────────┘
```

## 📁 文件系統結構

```
H:\MemberSystem\nobappGitHub\
├── 📄 NOBApp.csproj
│   └─ 包含 CustomActionsAfterPublish 目標
│
├── 📄 VersionInfo.cs
│   └─ public const string Version = "0.84.4";
│
├── 📄 UpdateVersion.ps1
│   └─ 自動增加版本號
│
├── 📄 PostBuildScript.ps1
│   └─ 上傳到 GitHub Release
│
├── 📄 ManualPublish.bat
│   └─ 手動批處理發佈
│
├── 📄 QUICK_START.md
│   └─ 快速開始指南
│
├── 📄 PUBLISH_GUIDE.md
│   └─ 詳細發佈指南
│
├── 📄 CHECKLIST.md
│   └─ 最終檢查清單
│
└── 📁 Properties\PublishProfiles\
    └─ 📄 FolderProfile.pubxml
       └─ 發佈設定檔（目標: C:\BOT\PS）

C:\BOT\
├── 📁 PS\
│   └─ NOBApp.exe, *.dll, ... (發佈輸出)
│
└── 📦 v0.84.4.zip
   └─ 發佈的完整應用（✨ 自動建立）
```

## 🔄 版本號遞推過程

```
第一次發佈        第二次發佈     第三次發佈
(版本 0.84.3)  (版本 0.84.4)          (版本 0.84.5)

VersionInfo.cs:       VersionInfo.cs:     VersionInfo.cs:
┌──────────────┐    ┌──────────────┐       ┌──────────────┐
│ 0.84.3    │      │ 0.84.4       │       │ 0.84.5       │
└──────────────┘      └──────────────┘       └──────────────┘
  │         │     │
  ▼   ▼     ▼
    ZIP:         ZIP:       ZIP:
┌──────────────┐      ┌──────────────┐       ┌──────────────┐
│v0.84.3.zip   │      │v0.84.4.zip   │       │v0.84.5.zip   │
└──────────────┘   └──────────────┘  └──────────────┘
     │  │          │
     ▼   ▼   ▼
  Release:           Release:            Release:
┌──────────────┐    ┌──────────────┐       ┌──────────────┐
│ v0.84.3      │      │ v0.84.4      │       │ v0.84.5      │
└──────────────┘      └──────────────┘    └──────────────┘

GitHub Release Page (https://github.com/TwIcePenguin/nobapp/releases)
┌────────────────────────────────────────────────────────┐
│ 最新    │
│ ┌─────────────────────────────────────────────────┐   │
│ │ v0.84.5 (最新)          │   │
│ │ ├─ v0.84.5.zip      │   │
│ │ └─ 發佈時間: 2025-01-23 10:30         │   │
│ └─────────────────────────────────────────────────┘   │
│      │
│ ┌─────────────────────────────────────────────────┐   │
│ │ v0.84.4          │   │
│ │ ├─ v0.84.4.zip         │   │
│ │ └─ 發佈時間: 2025-01-23 09:15          │   │
│ └─────────────────────────────────────────────────┘   │
│ │
│ ┌─────────────────────────────────────────────────┐   │
│ │ v0.84.3           │   │
│ │ ├─ v0.84.3.zip   │   │
│ │ └─ 發佈時間: 2025-01-23 08:00        │   │
│ └─────────────────────────────────────────────────┘   │
│          │
│ 較舊  │
└────────────────────────────────────────────────────────┘
```

## 🔌 PowerShell 脚本流程

### UpdateVersion.ps1
```
Input: VersionInfo.cs (內容: Version = "0.84.3")
         │
         ▼
┌─────────────────────────────────┐
│ 使用正則表達式提取版本          │
│ Pattern: 'public\s+const\s+string\s+Version\s*=\s*"([^"]+)"'
│ Result: majorVersion=0, minorVersion=84, buildNumber=3
└─────────────────────────────────┘
   │
       ▼
┌─────────────────────────────────┐
│ 增加版本號       │
│ buildNumber++ → buildNumber=4   │
│ newVersion = "0.84.4"   │
└─────────────────────────────────┘
    │
         ▼
┌─────────────────────────────────┐
│ 寫回到 VersionInfo.cs            │
│ 新內容: Version = "0.84.4"      │
│ 編碼: UTF-8        │
└─────────────────────────────────┘
         │
         ▼
Output: VersionInfo.cs (已更新)
        "0.84.4" (輸出到調用端)
```

### PostBuildScript.ps1
```
Input: ZIP 檔案路徑, 版本號, GitHub Token
      │
         ▼
┌─────────────────────────────────┐
│ 驗證輸入       │
│ ├─ ZIP 檔案是否存在 ✓           │
│ ├─ GitHub Token 是否設定 ✓      │
│ └─ Git 是否可用 ✓  │
└─────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────┐
│ Git 操作         │
│ git add VersionInfo.cs          │
│ git commit -m "Release v0.84.4" │
│ git push origin main      │
└─────────────────────────────────┘
  │
         ▼
┌─────────────────────────────────┐
│ 建立 GitHub Release              │
│ POST /repos/.../releases      │
│ tag_name: "v0.84.4"             │
│ name: "Release v0.84.4"         │
└─────────────────────────────────┘
   │
         ▼
┌─────────────────────────────────┐
│ 上傳 ZIP 檔案         │
│ POST upload_url          │
│ Content-Type: application/zip   │
└─────────────────────────────────┘
      │
       ▼
Output: Release URL: https://github.com/TwIcePenguin/nobapp/releases/tag/v0.84.4
        Download URL: https://github.com/.../download/v0.84.4/v0.84.4.zip
```

## 💾 環境變數

```
在發佈前設定以下環境變數（PowerShell）:

$env:GITHUB_TOKEN = "ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

或者永久設定（系統環境變數）:
[Environment]::SetEnvironmentVariable("GITHUB_TOKEN", "ghp_...", "User")

驗證設定:
$env:GITHUB_TOKEN  # 應該顯示 Token 值

驗證 Token 有效性:
$headers = @{"Authorization" = "token $env:GITHUB_TOKEN"}
Invoke-RestMethod -Uri "https://api.github.com/user" -Headers $headers
# 應該返回 GitHub 用戶信息
```

## ✨ 功能特性

```
✅ 完全自動化
   └─ 一鍵發佈，自動完成所有步驟

✅ 版本號管理
   └─ 自動遞增，無需手動修改

✅ GitHub 集成
   └─ 自動建立 Release、上傳 ZIP、推送代碼

✅ 錯誤處理
   └─ 詳細的錯誤信息和日誌輸出

✅ 跨平台相容
   └─ PowerShell、批處理、命令行都支持

✅ 可定製化
   └─ 易於修改 GitHub 倉庫、發佈目錄等參數
```

---

**架構版本**: 1.0
**最後更新**: 2025-01-23
**狀態**: ✅ 生產就緒
